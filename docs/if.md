# If statements in Lascal
If statements in Lascal looks like this:

    if (statement) {
        // what I do
    }
If statements are functions that are only ran when the statement is triggered. After that the function is cleaned up.